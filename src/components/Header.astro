---
import Container from "./Container.astro";
import InboxDropdown from "./inbox-dropdown/Dropdown.svelte";

const mainLinks = [
  {
    label: "👱 Tentang",
    href: "/",
  },
  {
    label: "✍️ Tulisan",
    href: "/posts",
  },
  {
    label: "🗒️ Resume",
    href: "/resume",
  },
];
---

<header
  class="py-5 border-b border-primary border-dashed bg-gradient-to-b from-primary/20"
>
  <Container
    class="flex md:items-center justify-between flex-col gap-6 md:flex-row"
  >
    <div class="flex items-center justify-between">
      <a href="/" class="font-bold text-2xl flex items-center gap-1.5">
        Adithya
        <picture>
          <source
            srcset="https://fonts.gstatic.com/s/e/notoemoji/latest/1f44b/512.webp"
            type="image/webp"
          />
          <img
            src="https://fonts.gstatic.com/s/e/notoemoji/latest/1f44b/512.gif"
            alt="👋"
            width="32"
            height="32"
          />
        </picture>
      </a>
      <InboxDropdown client:visible />
    </div>
    <nav>
      <ul class="flex items-center gap-6">
        {
          mainLinks.map((link) => (
            <li>
              <a href={link.href}>{link.label}</a>
            </li>
          ))
        }
      </ul>
    </nav>
  </Container>
</header>
